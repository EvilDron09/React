export const FormComponent = () => {
    return (
        <div>
            <form>
                <input type="text" name={'name'}/>
                <input type="text" name={'password'}/>
            </form>
        </div>
    );
};
