import {LoginComponent} from "../components/login-component/LoginComponent.tsx";

// Сторінка логінації
export const LoginPage = () => {
    return (
        <>
            <LoginComponent/>
        </>
    );
};
